# ocr Invariants

- Contract original is temporary only.
- Raw unmasked OCR text must not be persisted when it contains sensitive data.
- OCR failures must have fixture/manual-text fallback for demo stability.
